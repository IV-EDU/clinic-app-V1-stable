All agent behavior and project-specific rules are defined in `AGENTS.md` at the project root.

When working in this repository:
- Read `AGENTS.md` first and follow it strictly.
- `AGENTS.md` will tell you when to consult `MEMORY.md`, `LAST_PLAN.md`, `KNOWN_ISSUES.md`, `DESIGN_BRIEF.md`, and `docs/INDEX.md`.

This file is intentionally minimal to avoid conflicting instructions.
